import borderSource from "../build/js/border";
import colorSource from "../build/js/color";
import fontSource from "../build/js/font";
import opacitySource from "../build/js/opacity";
import shadowSource from "../build/js/shadow";
import spacingSource from "../build/js/spacing";

const base = {
  border: borderSource.border,
  color: colorSource.color,
  font: fontSource.font,
  opacity: opacitySource.opacity,
  shadow: shadowSource.shadow,
  spacing: spacingSource.spacing,
};

export default base;
